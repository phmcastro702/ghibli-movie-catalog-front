import styled from "styled-components";
import { MoviesList } from '../../Components/MoviesList';

const Catalog = () => {
    return (
        <>
            <h1>Catalog Page</h1>
            <MoviesList />
        </>
    );
};

export { Catalog };