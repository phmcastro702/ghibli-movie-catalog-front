import styled from "styled-components";
import { MoviesList } from '../../Components/MoviesList';

const Catalog = () => {
    return (
        <>            
            <MoviesList />
        </>
    );
};

export { Catalog };